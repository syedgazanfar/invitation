/**
 * Custom React Hooks
 * 
 * This module exports all custom hooks used in the application.
 */

// WebSocket Hook
export { 
  useWebSocket, 
  type WebSocketState,
  type WebSocketMessage,
  type WebSocketData,
  type ApprovalUpdateMessage,
  type PendingCountMessage,
  type NewUserMessage,
  type OrderUpdateMessage,
  type ConnectionMessage,
} from './useWebSocket';

// Add more hooks here as needed
